export interface NoteBody {
  title: string,
  text: string;
}

export interface ReqNoteBody {
  title: string,
  text: string;
}

export interface UNotes {
  title: string,
  text: string,
  pinned: boolean;
}